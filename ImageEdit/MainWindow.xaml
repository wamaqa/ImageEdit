<Window x:Class="ImageEdit.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageEdit"
        mc:Ignorable="d" Loaded="MainWindow_OnLoaded" Closed="MainWindow_OnClosed"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <Border BorderBrush="Gainsboro" BorderThickness="0 0 1 0">
            <Image Name="Image" Grid.Column="0" OpacityMask="#FF402727">
                <Image.ContextMenu>
                    <ContextMenu>
                        <MenuItem   Header="打开文件" Click="MenuOpenFile_OnClick"/>
                    </ContextMenu>
                </Image.ContextMenu>
            </Image>
        </Border>
        <Grid Grid.Column="1">
            <Button>
                <Button.ContextMenu>
                    <ContextMenu>
                        <MenuItem  Name="MenuOpenFile" Header="打开文件" Click="MenuOpenFile_OnClick"/>
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>
        </Grid>
    </Grid>
</Window>
